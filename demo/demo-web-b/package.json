{
  "name": "demo-web-b",
  "private": true,
  "type": "module",
  "scripts": {
    "build:sdk": "pnpm --dir ../../sdk/web-ts build",
    "dev": "pnpm run build:sdk && vite --force --host 127.0.0.1 --port 5174 --strictPort",
    "build": "pnpm run build:sdk && tsc -b && vite build",
    "preview": "vite preview --port 5174 --strictPort"
  },
  "dependencies": {
    "@unified-login/web": "link:../../sdk/web-ts"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vite": "^7.0.0"
  }
}
